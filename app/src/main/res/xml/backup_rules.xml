<?xml version="1.0" encoding="utf-8"?>
<backup-rules>
    <!-- Exclude all data from legacy backup (Android 11 and below) -->
    <exclude domain="sharedpref" path="." />
    <exclude domain="database" path="." />
    <exclude domain="file" path="." />
</backup-rules>
